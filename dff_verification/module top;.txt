module top;

    // Interface instance
    dff_if intf();

    // DUT instance
    dff dut(intf);

    // Test handle
    test t;
   initial begin
        intf.clk = 0;
        forever #5 intf.clk = ~intf.clk;
    end
  
   initial begin

        // Create test object
        t = new(intf, intf);

        // Run test
        t.run();
   end
   initial begin
        #200;
        $finish;
    end

endmodule